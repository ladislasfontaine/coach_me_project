<%= form_tag user_path(params[:id]), method: "put"  do %>

  <%= select_tag "objective", options_from_collection_for_select(@objectives, "id", "description") %>
  <%= submit_tag "Modifier", class: "btn btn-primary" %>

<% end %>