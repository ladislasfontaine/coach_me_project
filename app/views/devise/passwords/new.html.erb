<div class="background_form">
	 <%= image_tag("background-form.jpg", class:'background_form_img', alt:"fond d'image") %>
</div>

<div class="d-flex justify-content-center align-items-center mb-5 pb-5">
  <div class="box-form-sm text-center">

    <h1 class="orange">CoachMe</h1>
    <hr>
    <h4 class="mt-3">Mot de passe oublié ?</h4>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  		<%= render "devise/shared/error_messages", resource: resource %>

      <div class="field form my-2">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true %>
        <label for="email" class="label-name">
          <span class="content-name"><%= f.label :email %><br /></span>
        </label>
      </div>

      <div class="actions mb-5">
        <%= f.submit "Envoyer moi les instructions", class:"btn btn-primary mt-3" %>
      </div>

    <% end %>

    <hr>
    <div class="decoration-none mt-3">
      <%= render "devise/shared/links"%>
    </div>
  
  </div>
</div>